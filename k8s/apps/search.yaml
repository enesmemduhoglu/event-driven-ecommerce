apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-api
  namespace: ecommerce
  labels:
    app: search-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: search-api
  template:
    metadata:
      labels:
        app: search-api
    spec:
      containers:
        - name: search-api
          image: ghcr.io/enesmemduhoglu/event-driven-ecommerce/search-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: shared-config
          env:
            - name: Elasticsearch__Url
              value: http://elasticsearch:9200
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 12
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: search-api
  namespace: ecommerce
spec:
  selector:
    app: search-api
  ports:
    - port: 8080
      targetPort: 8080
