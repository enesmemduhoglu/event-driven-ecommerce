apiVersion: apps/v1
kind: Deployment
metadata:
  name: basket-api
  namespace: ecommerce
  labels:
    app: basket-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: basket-api
  template:
    metadata:
      labels:
        app: basket-api
    spec:
      containers:
        - name: basket-api
          image: ghcr.io/enesmemduhoglu/event-driven-ecommerce/basket-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: shared-config
          env:
            - name: ConnectionStrings__Redis
              value: redis:6379
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 12
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: basket-api
  namespace: ecommerce
spec:
  selector:
    app: basket-api
  ports:
    - port: 8080
      targetPort: 8080
