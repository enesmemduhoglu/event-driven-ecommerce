# DEMO ONLY: real deployments must not commit credentials to git —
# use a secret manager (e.g. external-secrets, sealed-secrets) instead.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: ecommerce
type: Opaque
stringData:
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
